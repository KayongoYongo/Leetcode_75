General approach is to create an empty string.
Next is to find the maximum length compared to each string.
Next, iterate through each string adding a character on the condition that the length of the string is less than the maximum value.
Return the final string.
